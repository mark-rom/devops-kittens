services:
  db:
    image: postgres:15
    ports:
     - "5432"
    environment: 
      - ./database.env

  app:
    image: pavelsergeev/kittens-store:${IMAGE_TAG:-latest}
    # build:
    #   context: ../.
    #   dockerfile: Dockerfile
    depends_on:
      - db
    ports:
      - "1234:1234"
    env_file: 
      - ./app.env
