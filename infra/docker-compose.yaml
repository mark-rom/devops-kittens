services:
  db:
    image: postgres:15
    ports:
      - 5432:5432
    env_file: 
      - .env

  app:
    image: pavelsergeev/kittens-store:${IMAGE_TAG:-latest}
    # build:
    #   context: ../.
    #   dockerfile: Dockerfile
    depends_on:
      - db
    ports:
      - "1234:1234"
    env_file: 
      - .env
